<!DOCTYPE html>
<html lang="ja">
<head>
    <meta charset="UTF-8">
    <title>{{ root.title }}; URI Shortener</title>
</head>
<body>
    <header>
        <h1>短縮URLサービス {{ root.title }} にようこそ</h1>
    </header>
    <section id="main">
        <h2>当サイトについて</h2>
        <p>
            当サイトは短縮URLサービスです。
        </p>
    </section>
    <section id="submit">
        <p>
            以下でURLが生成できます。
        </p>
        <form method="post" action="{{ url_for('accept_post') }}">
            <input type="url" name="uri" required>
            <input type="checkbox" name="jamp_flag">自動ジャンプせず警告ページを表示する。
            <button type="submit" value="submit">送信する</button>
        </form>
    </section>
</body>
</html>
